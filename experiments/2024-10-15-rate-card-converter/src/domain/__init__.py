"""Domain layer: entities, ports, and domain exceptions.

This layer contains pure business logic with no dependency on external frameworks,
databases, or cloud services. All infrastructure interactions are defined as
abstract port interfaces (ABCs) in the ports sub-package.
"""
