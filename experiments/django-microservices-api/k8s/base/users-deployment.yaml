apiVersion: apps/v1
kind: Deployment
metadata:
  name: users-service
  namespace: django-platform
spec:
  replicas: 2
  selector:
    matchLabels:
      app: users-service
  template:
    metadata:
      labels:
        app: users-service
    spec:
      containers:
        - name: users-service
          image: <aws_account_id>.dkr.ecr.<region>.amazonaws.com/users-service:latest
          ports:
            - containerPort: 8000
          envFrom:
            - secretRef:
                name: platform-secrets
          readinessProbe:
            httpGet:
              path: /api/v1/health/
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 15
---
apiVersion: v1
kind: Service
metadata:
  name: users-service
  namespace: django-platform
spec:
  selector:
    app: users-service
  ports:
    - port: 80
      targetPort: 8000
