# Scripts

Utility scripts that streamline workflows go here. Keep them small, documented, and cross-platform when possible. Current helpers:

- `new-experiment.ps1` — scaffolds a new experiment directory with standard files.

Add usage notes inside each script so they are self-explanatory.
